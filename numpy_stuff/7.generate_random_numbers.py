import numpy as np

# Generate a random number between 0 and 1
random_num = np.random.rand()
print(random_num)

# Generate an array of random integers between 0 and 9 (inclusive)
random_array = np.random.randint(0, 10, size=5)
print(random_array)